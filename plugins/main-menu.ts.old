import { Plugin,PluginContext } from "@roidev/kachina-md";
export default {
  name: 'menu',
  commands:['menu','help'],
  category: 'general',
  description: 'Menampilkan daftar perintah yang tersedia',

  async exec({ m, client, prefix }: PluginContext) {
    const menuText = `
╭───────────────────
│  📱 *WHATSAPP BOT*
│  ────────────────
│  👤 Owner : ${Array.isArray(client.config.owner) ? client.config.owner.join(', ') : client.config.owner}
│  🔧 Prefix : ${prefix}
╰───────────────────

╭───────────────────
│  📋 *MENU UMUM*
│  ────────────────
│  ${prefix}menu
│  ${prefix}ping
│  ${prefix}info
│  ${prefix}sticker
╰───────────────────

╭───────────────────
│  🎉 *MENU FUN*
│  ────────────────
│  ${prefix}quote
│  ${prefix}motivasi
╰───────────────────

💡 *Tips:* Kirim ${prefix}namacommand untuk menggunakan
    `.trim();

    await m.reply(menuText);
  }
} satisfies Plugin;
